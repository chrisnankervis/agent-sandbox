schemaVersion: "1"
kind: mixin
name: personal-codex-config
displayName: Personal Codex configuration
description: Installs prepared Codex instructions, Git identity, and SSH signing configuration in the sandbox agent's home directory
